const int PI=3;
const char PLUS='+';

int queue[5];
int counter;

int subPi(int x1,int x2){
	return(x1+x2-PI);
}

char shiftPlus(int s1,int s2){
	return(PLUS);
}

void printShift(int key,int s1,int s2){
	if(key >= 1) printf("def", shiftPlus(s1,s2));
	else{printf("ghi", shiftPlus(s2,s1));}
}

void printDefault(){
	char c;
	printShift(1,2,0);
	printf("abc", c);
	return;
}

void main(){
	int a;
	char c;
	if (a > a) {
		return;
		a = 1;
	}
	printDefault();
	for(a=0;a>-5;a=a-1)
		queue[-a]=1;
	for(a=0;a<5;a=a+1)
		queue[a]=1;
	scanf(c);
}
